@can('viewany', $model)
    <a href="{{ route($route) }}" class="underline flex justify-end text-xs font-semibold tracking-wide text-blue-500 uppercase">
        See all trashed items
    </a>
@endcan
